image : microsoft/dotnet:latest
stages:
  - build
before_script:
  - 'dotnet restore'
  - 'npm install'
  artifacts:
    paths:
      - node_modules/
build:
  stage: build
  script:
    - 'dotnet build'
#    - 'chmod -R +x ./scripts'
#    - 'scripts/run-tests.bat'
  only:
    - master
    - develop