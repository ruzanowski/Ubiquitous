stages:
  - deploy_common

common:
  stage: deploy_common
  image: docker:latest
  variables:
    DOCKER_DRIVER: overlay
  services:
    - docker:dind
  before_script:
    - chmod -R a+x scripts
  script:
    - ./scripts/dotnet-nuget-push.sh
    - ./scripts/dotnet-publish.sh
